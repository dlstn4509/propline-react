import React from 'react';
import styled from '@/style';
import { Link } from 'react-router-dom';

const SearchPublicMyCp = () => {
  return <div>SearchPublicMyCp</div>;
};

export default React.memo(SearchPublicMyCp);
